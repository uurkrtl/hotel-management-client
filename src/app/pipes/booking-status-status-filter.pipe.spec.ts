import { BookingStatusStatusFilterPipe } from './booking-status-status-filter.pipe';

describe('BookingStatusStatusFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BookingStatusStatusFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
