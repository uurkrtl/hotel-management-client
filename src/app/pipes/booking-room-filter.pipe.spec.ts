import { BookingRoomFilterPipe } from './booking-room-filter.pipe';

describe('BookingRoomFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BookingRoomFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
