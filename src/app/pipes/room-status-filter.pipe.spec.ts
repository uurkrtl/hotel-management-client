import { RoomStatusFilterPipe } from './room-status-filter.pipe';

describe('RoomStatusFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RoomStatusFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
